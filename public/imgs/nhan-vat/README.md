# Spritesheet nhân vật

Mỗi file là spritesheet dùng bởi Phaser. Tên file mặc định trùng `character.key`.

Kích thước frame và dải frame animation được khai báo trong `src/nhan-vat/configs.js`. Nếu thay ảnh, phải cập nhật `frameWidth`, `frameHeight` và các dải `idle/run/jump/attack/skill`.
