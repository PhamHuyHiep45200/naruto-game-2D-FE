# Asset projectile chưởng

Ảnh trong thư mục này được Phaser tải tại `LangLaScene.preload()` và bắn tới mục tiêu trong `castSkill()`.

- `rasengan.png`: SEN-JIN.
- `chuong-rong.png`: UCHY-HA.
- `chuong-ho.png`: HYU-GA.

Khóa texture được cấu hình bằng `skillImage` trong `src/nhan-vat/configs.js`.
