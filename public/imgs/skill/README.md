# Icon kỹ năng UI

Mỗi gia tộc có một thư mục chứa:

- `dam.png`: icon kỹ năng đấm.
- `chuong.png`: icon kỹ năng chưởng.

Ánh xạ gia tộc sang thư mục nằm tại `src/ui/constants.js`. Các ảnh này chỉ dùng trong HTML UI, không phải projectile Phaser.
