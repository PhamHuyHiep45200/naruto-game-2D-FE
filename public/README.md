# Thư mục `public`

Asset tĩnh được Vite phục vụ trực tiếp từ root URL.

Ví dụ file `public/imgs/common/shuriken.png` được dùng bằng đường dẫn `/imgs/common/shuriken.png`.

Không import asset trong `public` bằng JavaScript module. Xem `imgs/README.md` để biết quy ước từng nhóm ảnh.
